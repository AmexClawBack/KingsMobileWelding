backend:
  name: github
  repo: AmexClawBack/KingsMobileWelding
  branch: main
  base_url: https://netlify-cms-github-oauth-provider-cdue.onrender.com
  auth_endpoint: /auth

media_folder: "assets/uploads"   # files stored in repo
public_folder: "/assets/uploads" # how files are referenced on pages

publish_mode: simple  # (use 'editorial_workflow' if you prefer review/PRs)



collections:
  - label: "Blog"
    name: "blog"
    folder: "_posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    extension: "md"
    format: "frontmatter"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Publish Date", name: "date", widget: "datetime" }
      - { label: "Description", name: "description", widget: "text", required: false }
      - { label: "Cover Image", name: "cover", widget: "image", required: false }
      - { label: "Tags", name: "tags", widget: "list", required: false }
      - { label: "Body", name: "body", widget: "markdown" }
